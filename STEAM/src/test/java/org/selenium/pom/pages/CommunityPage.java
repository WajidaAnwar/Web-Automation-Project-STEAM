/*

package org.selenium.pom.pages;

        import org.openqa.selenium.By;
        import org.openqa.selenium.WebElement;
        import org.selenium.pom.base.BaseForm;
        import org.selenium.pom.base.Browser;
        import org.selenium.pom.elements.Button;

public class CommunityPage extends BaseForm {
    private Button signInButton = new Button(By.xpath("//span[normalize-space()='Sign In']"));
    private Button joinStreamButton = new Button(By.xpath("//span[normalize-space()='Join Steam']"));

    public CommunityPage(By elementLocator) {

        super(signInButton);
    }


    public boolean isSignInButtonDisplayed(){
        return signInButton.isDisplayed();
    }

    public void clickSignInButton(){
        signInButton.click();
    }

    public void clickJoinStreamButton(){
        joinStreamButton.click();
    }
}


*/
